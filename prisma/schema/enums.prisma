enum UserType {
  OWNER
  MEMBER
}

enum UserStatus {
  ACTIVE
  SUSPENDED
  INVITED
}

enum WorkspaceStatus {
  ACTIVE
  SUSPENDED
  ARCHIVED
}

enum MfaMethod {
  EMAIL
  AUTHENTICATOR
}

enum EmailReservationStatus {
  PENDING
  ACTIVE
  /// User deleted on shard; control-plane release incomplete (email not yet reusable)
  RELEASE_PENDING
}
