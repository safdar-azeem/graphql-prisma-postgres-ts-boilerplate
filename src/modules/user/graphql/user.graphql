type MfaSettings {
  isEnabled: Boolean!
  method: TwoFactorMethod
}

type User {
  id: ID!
  email: String!
  username: String!
  mfaSettings: MfaSettings
  createdAt: DateTime!
  updatedAt: DateTime!
}

# User Queries
extend type Query {
  me: User
}
